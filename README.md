# OuraCYD

A display project for the CYD (ESP32-2432S028R) dev board. Early days; see
`CLAUDE.md` for everything known about the hardware and the toolchain that
works with it. The sibling project (a live plane tracker on the same model
of board) lives in the planes repo under `esp32/`.
